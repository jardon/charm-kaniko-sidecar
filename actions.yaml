# Copyright 2021 jarred
# See LICENSE file for licensing details.
#
# TEMPLATE-TODO: change this example to suit your needs.
# If you don't need actions, you can remove the file entirely.
# It ties in to the example _on_fortune_action handler in src/charm.py
#
# Learn more about actions at: https://juju.is/docs/sdk/actions

add-job:
  description: Adds job to queue.
  params:
    context:
      description: "Git URL address for repository"
      type: string
      default: ""
    dockerfile: 
      description: "Relative path to dockerfile in repository"
      type: string
      default: ""
    destination:
      description: "Docker Hub repository URL"
      type: string
      default: ""
